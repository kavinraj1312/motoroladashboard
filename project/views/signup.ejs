<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Add some basic styling to the page */
        body {
      background-image: linear-gradient(90deg, #00DBDE 0%, #FC00FF 100%);
      background-size: 200% 100%; /* double the width of the body */
      background-position: 0% 0%; /* initial position */
      transition: background-position 10s ease-in-out; /* transition effect */
      animation: moveGradient 10s infinite; /* animation to move the gradient */
      font-family: 'Source Sans Pro Topnav', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #00DBDE;
      letter-spacing: 1px;
    }

    @keyframes moveGradient {
      0% {
        background-position: 0% 0%;
      }
      50% {
        background-position: 100% 0%;
      }
      100% {
        background-position: 0% 0%;
      }
    }

        h1{
            text-align: center;
            font-size: 20px;
            color:white
        }

        /* Center the content */
        .container {
            max-width: 400px;
            margin: 40px auto;
            padding: 20px;
           
            height: 400px;
            box-shadow: 0 0 10px rgba(251, 250, 250, 0.95);
            border-radius: 10px;
            background: transparent;
            justify-content: center;
            align-items: center;
        }

        form{
            align-items: center;
            justify-content: center;
        }

        /* Style the form elements */
        label {
            display: block;
            margin-bottom: 10px;
            color: white;
            text-align: center;
        }

        input[type="text"], input[type="password"] {
            margin-left: 40px;
            margin-top:10px;
            width: 300px;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            align-items: center;
            text-align: center;
        }

        button[type="submit"] {
            margin-top:10px;
            margin-bottom: 10px;
            margin-left: 40px;
            background-color: #4CAF50;
            color: #fff;
            padding: 10px 20px;
            border: none;
            width: 320px;
            border-radius: 5px;
            cursor: pointer;
            bottom: 4px;
        }

        button[type="submit"]:hover {
            background-color: #3e8e41;
           
        }
        

        /* Style the link */
        a {
            text-decoration: none;
            color: #dfe6eb;
            margin-left: 40px;
        }

        a:hover {
            color: #23527c;
        }
    </style>
</head>
<body>
  
    <div class="container">
        <h1>SIGNUP</h1>
    <form action="/signup" method="post">
        <label>USERNAME</label>
        <input type="text" name="username" placeholder="Enter your name" required>
        <label>PASSWORD</label>
        <input type="password" name="password" placeholder="Enter your password" required>
        <label>SECRETKEY</label>
        <input type="password" name="secretkey" placeholder="Enter the secret key" required>
        <button type="submit">SUBMIT</button>
    </form>
    <a href="/">Already have an account?</a>
</div>
    
</body>
</html>